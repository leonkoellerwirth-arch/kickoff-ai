## What changes

<!-- One sentence: what does this PR do? -->

## Affected level / module

<!-- Which bootstrap level (0–3) and module(s) does this touch? -->
<!-- Example: Level 2, module 07-ai-stack -->

## Checklist

- [ ] `bash -n <script>` passes for all changed scripts
- [ ] `shellcheck <script>` is clean (no warnings, no ignored rules added)
- [ ] `automation/bin/up2date --consistency --offline` exits 0
- [ ] Documentation updated (relevant `docs/` file)
- [ ] No personal data: no email addresses, no `/Users/<name>/` paths, no tokens
- [ ] `--dry-run` works correctly if the script changes system state

## Notes

<!-- Anything the reviewer should know that is not obvious from the diff -->
